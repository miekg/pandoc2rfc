pandoc2rfc (2.0)

*  Semi rewrite for XML2RFC v2.x

 -- Miek Gieben <miek@miek.nl>

pandoc2rfc (0.8.10)

 * Documentation

 -- Miek Gieben <miek@miek.nl>  

pandoc2rfc (0.8.9)

 * Uppercase Roman numerals convert to (1), (2), etc. instead of I. II. etc.
 * Caption are now typeset as postamble (was preamble). This look better in 
   the text output.

 -- Miek Gieben <miek@miek.nl>  

pandoc2rfc (0.8.8)

 * Bugfixes in how table width lookups are done (i.e. use the correct table).
 * Misuse footnote ([^1]) as iref.

 -- Miek Gieben <miek@miek.nl>  

pandoc2rfc (0.8.7)

 * If a figure has a caption, center it, otherwise leave it left aligned.
 * <blockquote> is converted to hanging list with one paragraph. 

 -- Miek Gieben <miek@miek.nl>  

 
pandoc2rfc (0.8.6)

 * Dropped AsciiDoc
 * Make it possible to reference figure/artworks and tables, in very much
   the same way as referencing sections.

 -- Miek Gieben <miek@miek.nl>  

 
pandoc2rfc (0.8.5)

 * Use the default list markers (#.) to signal a <list style="empty">
   paragraph.

 -- Miek Gieben <miek@miek.nl>  


pandoc2rfc (0.8.4)

 * Allow for multiple paragraphs (faked with vspace) in lists

 -- Miek Gieben <miek@miek.nl>  


pandoc2rfc (0.8.3)

 * Fix tables as generated by pandoc 1.9+ (informaltables)

 -- Miek Gieben <miek@miek.nl>  


pandoc2rfc (0.8.2)

 * Add `single-xml` to create one large XML file. For easy
    upload to the IETF.

 -- Miek Gieben <miek@miek.nl>  Sun, 01 Jan 2012 22:03:30 +0100


pandoc2rfc (0.8.1)

 * Assorted bugfixes.
 * Syntax coloring more in sync between Pandoc and AsciiDoc.
 * Added AUTHORS and CONTRIBUTORS file.

 -- Miek Gieben <miek@miek.nl>  Tue, 27 Dec 2011 20:32:01 +0100


pandoc2rfc (0.8)

 * Started a changelog.
 * Pandoc is supported as input language.
 * AsciiDoc is supported as input language.

 -- Miek Gieben <miek@miek.nl>  Sun, 25 Dec 2011 11:05:00 +0100
